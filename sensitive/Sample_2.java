package com.example;

/**
 * Sample class to test keyword/param scanning.
 */
public class Sample {

    // ─────────────────────────────────────────────────────────────
    // 1)  keyword YES, param YES   →  "keyword yes param yes"
    public void initProcess(String configSecret) {
        // llm
		int money = 99;
        int jackpot = 0;
        if (money != null) {
            jackpot++;
        }
    }
    }
}
